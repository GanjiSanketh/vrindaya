export interface PopupConfig {
  enabled:            boolean;
  productId:          number;
  showDelay:          number;
  showOncePerSession: boolean;
  title:              string;
  subtitle:           string;
}
