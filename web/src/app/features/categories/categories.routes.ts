import { Routes } from '@angular/router';

/**
 * Category browse routes — currently served by the products feature
 * (category/:id → ProductListingComponent). Reserved here for future
 * expansion: e.g. a dedicated category landing page at /categories
 * showing all category cards.
 */
export const CATEGORIES_ROUTES: Routes = [];
