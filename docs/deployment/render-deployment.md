# Deploying `api/` to Render

This guide moved to [render.md](render.md) when `api/` switched to a
Docker-based deployment (see [docker.md](docker.md) for the image
itself) — the Render service now builds from `api/Dockerfile` rather
than Render's native .NET runtime auto-detection this file used to
describe. Update any bookmarks/links to point at
[render.md](render.md) directly.
